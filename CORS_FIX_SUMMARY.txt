═══════════════════════════════════════════════════════════════════
  CORS FIX - QUICK STEPS (No Flutter Source Code Required)
═══════════════════════════════════════════════════════════════════

PROBLEM: 500 Internal Server Error on dealer login API
URL: https://uc1t4bgsq3.execute-api.ap-south-1.amazonaws.com/dev/dealer/dealerlogin

SOLUTION: Add CORS headers to your AWS Lambda function

───────────────────────────────────────────────────────────────────
STEP 1: Update Lambda Function
───────────────────────────────────────────────────────────────────

1. Go to AWS Console → Lambda
2. Find your dealer login function
3. Add CORS headers to ALL responses:

   headers: {
     'Access-Control-Allow-Origin': '*',
     'Access-Control-Allow-Methods': 'GET, POST, PUT, DELETE, OPTIONS',
     'Access-Control-Allow-Headers': 'Content-Type, Authorization, X-Requested-With',
     'Content-Type': 'application/json'
   }

4. Handle OPTIONS method (preflight):

   if (event.httpMethod === 'OPTIONS') {
     return {
       statusCode: 200,
       headers: { /* CORS headers */ },
       body: ''
     };
   }

5. Click "Deploy"

───────────────────────────────────────────────────────────────────
STEP 2: Enable CORS in API Gateway
───────────────────────────────────────────────────────────────────

1. Go to AWS Console → API Gateway
2. Find your API → /dealer/dealerlogin
3. Actions → Enable CORS
4. Set:
   - Allow Origin: *
   - Allow Headers: Content-Type,Authorization,X-Requested-With
   - Allow Methods: POST,OPTIONS
5. Click "Enable CORS and replace existing CORS headers"
6. Actions → Deploy API → Stage: dev

───────────────────────────────────────────────────────────────────
STEP 3: Test
───────────────────────────────────────────────────────────────────

Open your Netlify site and try dealer login again.

Check Network tab in browser DevTools for:
✓ Status: 200 (not 500)
✓ Response headers include: access-control-allow-origin: *

───────────────────────────────────────────────────────────────────
STEP 4: If Still Failing, Check CloudWatch Logs
───────────────────────────────────────────────────────────────────

AWS CloudWatch → Log groups → /aws/lambda/YOUR_FUNCTION_NAME

Look for:
- Missing environment variables
- Database connection errors
- Timeout errors
- Permission errors

───────────────────────────────────────────────────────────────────
📖 DETAILED GUIDE
───────────────────────────────────────────────────────────────────

Read: FIX_CORS_ON_AWS.md (in this folder)

───────────────────────────────────────────────────────────────────
✅ DONE!
───────────────────────────────────────────────────────────────────

After these changes:
• Lambda returns CORS headers
• API Gateway allows cross-origin requests
• Your Flutter web app on Netlify can call the API
• No need to modify or rebuild Flutter code

═══════════════════════════════════════════════════════════════════
